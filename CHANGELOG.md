# CHANGELOG — 报关资料自动生成

本文件记录本项目从联调到稳定上线的关键改动、问题复盘与可复用实施路径。
目标是让后续维护人员不依赖聊天上下文也能完整回溯。

---

## 如何用本文件做问题回溯

```bash
# 1) 确认服务运行状态
ssh root@101.96.212.128 "systemctl is-active baoguan"

# 2) 看后端系统日志
ssh root@101.96.212.128 "journalctl -u baoguan -n 200 --no-pager"

# 3) 看新增访问日志（请求路径/状态/耗时/d长度）
ssh root@101.96.212.128 "tail -n 200 /root/baoguan-backend/backend_access.log"

# 4) 快速探测下载链路
ssh root@101.96.212.128 "curl -s -o /dev/null -w '%{http_code}' 'https://pkcellsolution.com/baoguan/generate?d=eyJyb3dzIjpbXX0'"
```

---

## 迭代索引（时间倒序）

| 版本    | 日期       | 类型 | 说明                                                                               |
| ------- | ---------- | ---- | ---------------------------------------------------------------------------------- |
| pending | 2026-05-26 | fix  | 运费行识别补强：商品名称或规格型号为“国际运费”时均视为运费行，不写入商品明细       |
| pending | 2026-05-25 | fix  | 发票/合同支持超项自动扩行：明细公式连续重写，汇总公式按实际行数自动重定位          |
| pending | 2026-05-25 | fix  | 箱单 E 列数量单位公式连续联动：E10=发票!E9，后续行按序递增                         |
| pending | 2026-05-25 | fix  | 境内货源地标准化：深圳/珠海写“特区”，其他地区写“其他”                              |
| pending | 2026-05-25 | fix  | 前端净重字段兼容与备注修正：支持“净重/净重（千克）”，去除全局净重误报              |
| pending | 2026-05-25 | docs | 发票工作表OFFSET引用连续性复核：当前模板G1~G14连续，无G11跳G13问题                 |
| pending | 2026-05-22 | feat | 箱单H/I/J核对公式自动写入：锂离子写比能量核对链路、锂亚硫酰氯写灌装含量核对链路    |
| pending | 2026-05-22 | feat | 8506500011锂亚硫酰氯规则落地：固定材质文案 + 第11位灌装含量按箱单D/G与型号占比计算 |
| pending | 2026-05-22 | feat | 锂离子第11位自动计算比能量：按箱单D/G同一行计算并写入`比能量：NNN.NNWH/KG`         |
| pending | 2026-05-22 | feat | 新增箱单逐行联动：报关数据W/X/Y写入箱单C10/F10/G10起，动态SUM汇总并驱动E12/F12/G12 |
| pending | 2026-05-20 | feat | 国际运费按数量比例分摊到各商品总价并反推单价（总价2位/单价4位）                    |
| pending | 2026-05-20 | fix  | 报关单 E12/F12/G12 改为保留箱单联动公式（不再清空）                                |
| pending | 2026-05-20 | fix  | 报关资料列位映射调整：C=TraceId，D=时间，E=状态，F=失败，G=备注                    |
| pending | 2026-05-20 | feat | Debug体系上线：TraceId写入报关资料C列并贯穿前后端日志                              |
| pending | 2026-05-20 | feat | 后端新增结构化错误码（BG4001/BG4002/BG4003/BG5000）                                |
| pending | 2026-05-20 | fix  | 明细区格式统一：第8项后复制首块样式/合并布局，保持字体与间距一致                   |
| pending | 2026-05-20 | fix  | 业务主键切换为合同号码：前端A列与数据筛选改为合同号码                              |
| pending | 2026-05-20 | docs | README/项目计划书同步合同号码口径与明细固定字段规则                                |
| pending | 2026-05-20 | fix  | 移除前后端 d 参数长度拦截，应用层不再返回“数据过大”                                |
| pending | 2026-05-20 | docs | 明确链接有效期与长度边界：无时间过期机制，但受网关URI上限约束                      |
| pending | 2026-05-19 | fix  | 修复商品多时“102”漏写：每个商品块显式写入征免方式 102                              |
| pending | 2026-05-19 | fix  | 修正运费落位：由 L11 调整为 K12（合并单元格主单元格）                              |
| pending | 2026-05-19 | fix  | 放宽前端 URL 长度阈值至 30000，支持大订单生成链接                                  |
| pending | 2026-05-19 | fix  | 件数/毛重/净重及商品明细毛重改为人工填写（自动留空）                               |
| pending | 2026-05-19 | fix  | 动态项数：按商品数写入并删除未使用明细行，移除模板预置残留                         |
| pending | 2026-05-19 | fix  | 国际运费识别并写入 K12，商品名称优先取源字段                                       |
| pending | 2026-05-19 | fix  | 固定数据源为“报关数据”，结果表备注落 F 列，非阻断生成                              |
| pending | 2026-05-19 | fix  | 字段兼容：币别映射币制；后端校验币制/币别二选一，净重非必填                        |
| pending | 2026-05-19 | docs | 新增 README 与 CHANGELOG，固化搭建流程与排障经验                                   |
| pending | 2026-05-19 | fix  | 新增后端请求级访问日志，支持精确定位下载失败                                       |
| pending | 2026-05-19 | fix  | 切换为“纯同步脚本 + 点击链接下载”稳定方案                                          |
| pending | 2026-05-19 | fix  | CORS 三层重复头治理（Flask/nginx/CDN）                                             |
| pending | 2026-05-19 | fix  | 废弃同步 XHR，改造为异步请求（后因运行时限制废弃）                                 |

---

## 详细变更记录

### [pending] 2026-05-26 — fix: 运费行识别补强，避免误写入商品明细

背景

- 线上 TraceId `BG-20260526084349-522003-GNSSED` 显示 `row_count=3`，其中第 3 行业务上是国际运费行。
- 旧逻辑仅在“商品编号为空 且 商品名称=国际运费”时识别运费；若 `规格型号=国际运费` 而 `商品名称` 为空，则该行会误进入商品明细。

改动位置

- `scripts/backend_server.py`
- `模板填写规则`

改动内容

- 新增 `_is_freight_row(row)` 分类函数。
- 运费识别规则扩展为：
  - `商品编号` 为空；且
  - `商品名称=国际运费` 或 `规格型号=国际运费`。
- 命中后仅作为运费行参与：
  - `K12` 运费写入
  - 按数量占比分摊到商品总价/单价
- 不再进入 `item_rows`，因此不会写入报关单商品明细区。

验证

- 本地最小校验通过：
  - `商品名称=国际运费` → 识别为运费行
  - `规格型号=国际运费` → 识别为运费行
  - `商品编号` 非空 → 不识别为运费行
  - 普通商品行 → 不识别为运费行

### [pending] 2026-05-25 — fix: 发票/合同超项自动扩行与汇总公式重定位

背景

- 线上反馈：商品数量超过发票基础容量后，发票与合同存在公式跳号、汇总范围不随明细扩展的问题。

改动位置

- `scripts/backend_server.py`

改动内容

- 新增发票明细区标准化与动态扩行：
  - 按商品数量重写发票明细公式，保证 `C~H` 引用连续（如第 19/20 行不再跳号）。
  - 当商品数超过基础容量时，自动在汇总行前插入明细行并复制样式/合并区。
  - 发票汇总行公式动态重写为 `=SUM(H9:H{last_detail_row})`。
- 新增合同明细区标准化与动态扩行：
  - 按商品数量重写合同明细映射（`F/G/H` 对应发票明细）。
  - 超容量自动扩行并复制样式/合并区。
  - 合同汇总公式动态重写为 `=SUM(H19:I{last_detail_row})`。
- 新增工作表名标准化匹配：兼容模板中的 `发 票` / ` 合同` 等空白字符差异，避免规则未命中。

验证

- 本地 16 项商品：
  - 发票汇总：`=SUM(H9:H24)`
  - 合同汇总：`=SUM(H19:I34)`
  - 箱单汇总：`=SUM(C10:C25)` / `=SUM(F10:F25)` / `=SUM(G10:G25)`
- 本地 20 项商品：
  - 发票汇总：`=SUM(H9:H28)`
  - 合同汇总：`=SUM(H19:I38)`
- 线上实测（HTTP 200）同样通过。

### [pending] 2026-05-25 — fix: 箱单 E 列数量单位公式连续联动

背景

- 线上反馈：箱单 `E` 列（数量单位）未从 `E10` 向下连续延续，导致部分行缺少对发票单位的联动公式。

改动位置

- `scripts/backend_server.py`

改动内容

- 箱单明细清空阶段新增 `E` 列清理，避免历史残留干扰。
- 箱单明细写入阶段新增 `E` 列公式写入：
  - 第 1 条商品：`E10 = 发票!E9`
  - 后续按行递增：`E11 = 发票!E10`、`E12 = 发票!E11` ...

验证

- 本地/线上 16 项商品均验证通过：`E10~E25` 分别为 `=发票!E9` 到 `=发票!E24`，连续无断档。

### [pending] 2026-05-25 — fix: 境内货源地标准化写入

背景

- 业务要求：境内货源地字段需按固定口径输出，避免“深圳/珠海/东莞”等值格式不一致。

改动位置

- `scripts/backend_server.py`
- `模板填写规则`

改动内容

- 新增货源地标准化规则：
  - 深圳（含“深圳市”）-> `深圳特区`
  - 珠海（含“珠海市”）-> `珠海特区`
  - 其他地区 -> `<地区名>其他`（如“东莞”->`东莞其他`）
  - 已以“特区/其他”结尾的值保持不变。

验证

- 线上服务已部署并重启，规则生效。

### [pending] 2026-05-25 — fix: 前端净重字段兼容与备注误报修正

背景

- 数据源表头已采用“毛重/净重”（无单位），但历史脚本存在“净重（千克）”文案与检查残留，导致备注误报。

改动位置

- `scripts/dingtalk_demo.js`
- `模板填写规则`

改动内容

- 前端字段兼容：支持 `毛重/净重` 与 `毛重（千克）/净重（千克）` 双口径。
- 备注逻辑修正：
  - 移除全局“净重无有效值”强制提示。
  - 保留按需逐行提示（仅锂离子/锂亚硫酰氯计算场景）。
  - 提示文案统一为 `净重`。

验证

- 重新运行脚本后，备注不再出现全局净重误报。

### [pending] 2026-05-25 — docs: 发票公式跳号问题复核

背景

- 用户反馈发票工作表存在从 G11 跳到 G13 的疑似引用问题。

复核结果

- 已对两个模板（单商品/多商品）逐项扫描发票 `OFFSET(报关单!...)` 引用。
- 当前引用序列为 `G1~G14` 连续，`jump_count=0`，未复现跳号。

### [pending] 2026-05-22 — feat: 箱单 H/I/J 核对公式自动写入

背景

- 业务要求：
  - 锂离子订单写入 H/I/J 核对公式（H=G/D, I=容量\*电压/1000, J=I/H）。
  - 锂亚硫酰氯订单写入 H/I/J 核对公式（H=G/D, I=型号占比, J=H\*I）。
  - 其他订单 H/I/J 保持空白。

改动位置

- `scripts/backend_server.py`

改动内容

- 箱单明细清空阶段新增 H/I/J 列清空，避免旧值残留。
- 明细写入阶段按商品类型动态写入 H/I/J：
  - 锂离子：`H=G/D`，`I=容量*电压/1000`，`J=I/H`。
  - 锂亚硫酰氯：`H=G/D`，`I=型号占比`，`J=H*I`。
  - 其他类型：H/I/J 不写值，保持空白。

验证

- 锂离子样例：H10=`=G10/D10`，I10=`=2600.0*3.7/1000`，J10=`=I10/H10`。
- 锂亚硫酰氯样例：H10=`=G10/D10`，I10=`0.3202`，J10=`=H10*I10`。
- 其他类型样例：H10/I10/J10 均为空。

### [pending] 2026-05-22 — feat: 8506500011 锂亚硫酰氯第11位灌装含量自动计算

背景

- 业务要求：HS `8506500011` 的材质需固定为 `锂亚硫酰氯 ,亚硫酰氯填充小于1千克`，并在第 11 位填写单个电池灌装含量。

改动位置

- `scripts/backend_server.py`
- `README.md`

改动内容

- 固定材质文案：`锂亚硫酰氯 ,亚硫酰氯填充小于1千克`。
- 新增型号占比映射（ER14250/ER18505M 等 21 个型号）。
- 第 11 位按 `((净重/数量)*型号占比)` 自动计算，净重与数量取箱单对应商品行 `G`/`D` 列。
- 输出格式：`单个锂亚硫酰氯电池亚硫酰氯灌装含量NNN.NNNNKG`。

验证

- 样例：ER18505M、净重 56.84、数量 1000，自动写入 `单个锂亚硫酰氯电池亚硫酰氯灌装含量0.0182KG`。

### [pending] 2026-05-22 — feat: 锂离子第11位比能量自动计算

背景

- 业务要求：当材质为锂离子时，第十一位需按箱单同一商品行数据自动计算比能量并回填。

改动位置

- `scripts/backend_server.py`
- `模板填写规则`
- `README.md`

改动内容

- 新增锂离子第十一位自动计算：
  `比能量=((容量mAh*电压V)/1000)/(净重/数量)`。
- 计算数据取箱单同一商品行：`G` 列净重、`D` 列数量（第 1 条商品为 `G10/D10`，依次递增）。
- 结果保留 2 位小数，写入格式：`比能量：NNN.NNWH/KG`。
- 箱单明细写入阶段同步补写 `D` 列数量，确保计算数据源一致。

验证

- 锂离子样例（2600mAh、3.7V、净重 180、数量 1000）自动写入：`比能量：53.44WH/KG`。
- 非锂离子商品第十一位不追加比能量字段。

### [pending] 2026-05-22 — feat: 箱单逐行写入与动态汇总联动

背景

- 业务要求：箱数/毛重/净重按商品逐行写入箱单，商品超 11 条需自动扩行并保持样式；报关单通过 E12/F12/G12 动态联动箱单汇总行。

改动位置

- `scripts/backend_server.py`
- `scripts/dingtalk_demo.js`
- `README.md`

改动内容

- 前端脚本补充 `箱数`、`毛重（千克）`、`净重（千克）` 字段，并增加 W/X/Y 列位兜底抓取。
- 后端按商品逐行写入箱单：第 1 条到 `C10/F10/G10`，第 2 条到 `C11/F11/G11`，依此类推。
- 商品超过 11 条时，在原汇总行前自动插入明细行，并复制第 20 行样式及单行合并样式。
- 箱单汇总行 `C/F/G` 自动写入动态 `SUM` 公式（如 `=SUM(C10:C22)`）。
- 报关单 `E12/F12/G12` 动态指向箱单汇总行（如 `=箱单!C23`、`=箱单!F23`、`=箱单!G23`）。

验证

- 11 条及以内商品：汇总行保持 21，报关单联动 `C21/F21/G21`。
- 超过 11 条商品：汇总行自动下移（例如 13 条时为 23），报关单公式同步指向新汇总行。

---

### [pending] 2026-05-20 — feat: 国际运费按数量比例分摊到商品金额

背景

- 业务要求：当订单存在国际运费时，运费不能只体现在抬头运费栏，还需按商品数量比例分摊到每个商品的总价，并据此重算单价。

改动位置

- `scripts/backend_server.py`
- `README.md`
- `模板填写规则`

改动内容

- 保留运费写入 `K12`。
- 新增运费分摊：某商品分摊运费 = 总运费 \* (该商品数量 / 全部商品数量)。
- 分摊后总价 = 原总价（数量\*单价）+ 分摊运费，总价保留 2 位小数。
- 分摊后单价 = 分摊后总价 / 商品数量，单价保留 4 位小数。
- 分摊场景下，商品总价单元格写入分摊后的数值；无运费场景保持原总价公式逻辑。

验证

- 运费分摊后各商品总价之和 = 原总价之和 + 总运费（考虑 2 位小数舍入后由末项兜底）。

---

### [pending] 2026-05-20 — fix: 件数/毛重/净重改为箱单联动公式

背景

- 业务要求：报关单件数/毛重/净重不应被清空，需在人工填写箱单后自动同步到报关单。

改动位置

- `scripts/backend_server.py`
- `README.md`
- `模板填写规则`

改动内容

- 报关单 `E12` 设为公式 `=箱单!C21`。
- 报关单 `F12` 设为公式 `=箱单!F21`。
- 报关单 `G12` 设为公式 `=箱单!G21`。
- 文档口径同步：由“E12/F12/G12 自动留空”调整为“保留公式联动箱单”。

验证

- 生成文件中 `E12/F12/G12` 均为公式，不再被置空。

---

### [pending] 2026-05-20 — feat: TraceId 全链路调试体系

背景

- 需要支持“同事报错后可精准定位到单次操作”的排障能力。

改动位置

- `scripts/dingtalk_demo.js`
- `scripts/backend_server.py`
- `README.md`

改动内容

- 前端每条生成记录创建 TraceId，并写入 `报关资料` C 列（按当前表头布局）。
- 下载链接附加 `trace_id` 参数，后端日志全链路记录该值。
- 后端新增结构化日志阶段：`REQ` / `BIZ` / `ERR` / `UNHANDLED`。
- 后端补充错误码：`BG4001`（缺少参数）、`BG4002`（解码失败）、`BG4003`（校验失败）、`BG5000`（未处理异常）。

验证

- 可通过 `grep trace_id=<TraceId>` 一次性串联同一请求的入口、业务、错误、响应日志。

经验总结

- 调试体系的关键是“可追踪ID + 结构化日志 + 错误码”，三者缺一不可。

---

### [pending] 2026-05-20 — fix: 报关资料工作表列位映射校正

背景

- 现场表头调整为：A 合同号码、B 下载链接、C TraceId、D 生成时间、E 状态、F 失败原因、G 备注。
- 为避免脚本回填错列，需要同步更新前端写入顺序与文档说明。

改动位置

- `scripts/dingtalk_demo.js`
- `README.md`
- `CHANGELOG.md`

改动内容

- 前端回填顺序改为：B 下载链接、C traceId、D 时间、E 状态、F 失败原因、G 备注。
- 超链接写入失败时，状态/失败原因改写入 E/F 列。
- 文档中的列位说明与排障检查项全部同步。

---

### [pending] 2026-05-20 — fix: 明细区格式统一（第8项后不再变形）

背景

- 商品项数较多时，第8项后的样式出现漂移（如 102 非红色、字体变小、单元格间距异常）。

改动位置

- `scripts/backend_server.py`

改动内容

- 新增首个明细块样式与合并布局复制逻辑，应用到全部商品槽位。
- 每个商品块继续显式写入固定字段（102、总价公式、照章征税、最终目的国）。

验证

- 第8项及后续项与前面项保持同样字体、颜色、边框、合并区和间距。

---

### [pending] 2026-05-20 — fix: 主键由系统号码切换为合同号码

背景

- 业务侧确认订单唯一标识改为合同号码，不再用系统号码。

改动位置

- `scripts/dingtalk_demo.js`
- `scripts/backend_server.py`

改动内容

- 前端读取 `报关资料` A 列按“合同号码”处理。
- 前端在数据源中按 `合同号码` 列筛选订单数据。
- 文案与错误提示统一替换为“合同号码”。
- 后端校验必填字段移除对 `系统号码` 的依赖。

验证

- A 列填写合同号码时可正常生成链接与下载文件。

---

### [pending] 2026-05-20 — fix: 移除前后端长度拦截

背景

- 大商品数订单在 URL 传参模式下容易触发应用层“数据过大”拦截。

改动位置

- `scripts/dingtalk_demo.js`
- `scripts/backend_server.py`

改动内容

- 前端移除 `MAX_URL_LENGTH` 判断与超长失败分支。
- 后端移除 `/generate` 与 `/gen` 对 `d` 参数 `len(d) > 20000` 的拦截。

验证

- 中等大参数（`d_len` 约 21067 / 26334）请求可返回 200，不再触发应用层“数据过大”。
- 超大 URL 仍会被网关/HTTP 层拒绝（可见 `414 Request-URI Too Large`）。

经验总结

- 代码层放开阈值可减少业务误拦截，但 URL 方案仍有基础设施物理上限。

---

### [pending] 2026-05-20 — docs: 链接有效期与长度边界说明

背景

- 需要统一口径回答“链接是否过期”和“是否无字符限制”。

改动位置

- `README.md`

改动内容

- 明确当前无“按时间过期”机制。
- 明确“代码层无固定长度限制”不等于“无限长 URL 可用”，仍受浏览器/CDN/nginx/HTTP 请求行长度限制。

经验总结

- 业务口径要区分“应用层规则”与“基础设施边界”，避免误判为系统不稳定。

---

### [pending] 2026-05-19 — fix: 修复商品多时“102”漏写

背景

- 商品明细较多时，部分商品块的“102”行未被填充，出现空白。

改动位置

- `scripts/backend_server.py`

改动内容

- 在商品块清理阶段增加 `B(base+1)` 清空。
- 在商品块写入阶段为每个商品显式写入 `B(base+1) = 102`。

验证

- 大商品数场景下，每个商品块对应行均可看到 `102`。

经验总结

- 固定业务值不能依赖模板预置，需由程序逐块显式写入，避免在大批量场景出现“前几行有、后几行无”的问题。

---

### [pending] 2026-05-19 — fix: 修正运费落位到 K12

背景

- 业务确认运费应填写在第 12 行的 K12 合并单元格，而不是标签后一格。

改动位置

- `scripts/backend_server.py`
- `README.md`
- `CHANGELOG.md`

改动内容

- 后端运费写入位置由 `L11` 调整为 `K12`（写合并单元格主单元格）。
- 文档说明同步更新，避免后续按旧坐标排查。

验证

- 运费样本生成后，运费值落在 `K12`。

经验总结

- 模板坐标变更要同步更新代码与文档，否则会出现“代码正确但验收口径不一致”的假故障。

---

### [pending] 2026-05-19 — fix: 放宽前端 URL 长度阈值

背景

- 同一订单下商品数较多时，前端在链接写入前触发“URL 超限”并提前失败。

改动位置

- `scripts/dingtalk_demo.js`

改动内容

- 新增可配置阈值 `MAX_URL_LENGTH`，默认值设为 30000。
- 超长判断由固定 7500 调整为读取该阈值。

验证

- 生成链接长度超过 7500 但小于 30000 的订单不再被前端直接拦截。

经验总结

- 前端阈值应与网关能力解耦并可配置，避免业务量增长时频繁改代码。

---

### [pending] 2026-05-19 — fix: 件数/毛重/净重与明细毛重改为人工填写

背景

- 业务要求调整：模板中的件数、毛重、净重以及商品明细毛重不再自动生成，改为人工录入。

改动位置

- `scripts/backend_server.py`

改动内容

- 模板头部值位清空：`E12`、`F12`、`G12`。
- 商品明细毛重值位清空：每个商品块 U 列值位（按 3 行步长）。
- 保留标题与结构，不影响其他字段填充。

验证

- 单商品/多商品生成文件均为 HTTP 200。
- `E12/F12/G12` 均为空。
- `U20/U23/U26` 等明细毛重值位均为空。

实施备注

- 改造初版曾触发 500：`MergedCell object attribute value is read-only`。
- 根因是写入了合并单元格的非主单元格。
- 修复后只清空可写值位主单元格，回归通过。

经验总结

- 对“模板展示字段”和“自动填报字段”要做明确边界，避免后续规则变动时影响整条链路。

---

### [pending] 2026-05-19 — fix: 动态项数写入与尾部明细清理

背景

- 多商品模板存在预置示例数据，曾出现“实际 2 条商品却显示更多项”的问题。

改动位置

- `scripts/backend_server.py`

改动内容

- 明细项按商品实际数量写入（A20 起，每项占 3 行）。
- 写入后删除未使用的占位行，避免模板残留项。

验证

- 单商品：仅 A20 为项号 1，A23 及后续为空。
- 双商品：A20=1、A23=2，A26 及后续为空。

经验总结

- 模板自带示例值时，必须在程序中显式清理或删除尾部行，不能依赖“覆盖写入”自然消除。

---

### [pending] 2026-05-19 — fix: 运费与商品名称填报规则对齐计划书

背景

- 计划书要求：
  - 运费由“国际运费”行识别并写入运费栏。
  - 商品名称优先使用源数据，不用 HS 映射兜底替代原值。

改动位置

- `scripts/backend_server.py`

改动内容

- 增加运费行识别：`商品编号` 为空且 `商品名称` 为 `国际运费`。
- 运费值写入 `K12`（合并单元格主单元格，格式：单价 + 币制/币别）。
- 商品名称字段优先取源数据行 `商品名称`。

验证

- 运费样本可稳定写入 `K12`。
- 商品名称按源字段显示。

经验总结

- 业务“例外行”（如运费行）应在后端统一识别处理，前端仅传原始数据，降低脚本复杂度。

---

### [pending] 2026-05-19 — fix: 固定数据源、备注落盘与非阻断策略

背景

- 早期读取“当前激活工作表”易误取数据，导致空白或错单。
- 现场需要“即使有缺列也先给可用下载入口”，不能一刀切失败。

改动位置

- `scripts/dingtalk_demo.js`

改动内容

- 数据源固定为 `报关数据`，结果表固定为 `报关资料`。
- 缺列/缺值不终止，继续生成链接。
- 备注信息写入 F 列，提示人工复核。

验证

- 数据源切换到非活动表时仍可正确生成。
- 缺列场景下 D 列可为“链接已生成”，F 列出现人工确认备注。

经验总结

- 自动化链路要优先保证“可执行”，再通过备注机制补充人工控制点。

---

### [pending] 2026-05-19 — fix: 币别/币制兼容与净重校验放宽

背景

- 源表历史字段使用“币别”，后端标准字段使用“币制”，曾导致字段校验失败。
- 净重在当前业务阶段改为人工填写，不应成为阻断项。

改动位置

- `scripts/dingtalk_demo.js`
- `scripts/backend_server.py`

改动内容

- 前端映射：当 `币制` 为空且 `币别` 有值时自动映射。
- 后端校验：`币制`/`币别` 二选一通过。
- 后端校验：净重不再作为必填字段。

验证

- 仅提供 `币别` 的数据可成功生成。
- 缺少净重时可成功生成并交由人工补录。

经验总结

- 跨系统字段命名不一致时，应在入口层做兼容，不把历史命名差异传递到业务层。

### 关键问题复盘（面向维护与交接）

### 问题 1：看见“服务是 active”，不代表“下载一定成功”

现象

- 服务状态是 active，但用户仍反馈“点下载没反应”或“只能下载一次”。

为什么会这样

- active 只说明进程活着，不说明每个请求都成功。
- 请求可能卡在 CDN、nginx、参数解析、文件返回任意一层。

如何快速确认

1. 看 systemd：确认进程没挂。
2. 看请求日志：确认每次请求是否返回 200。
3. 看客户端表现：是否真的发出了请求。

正确做法

- 必须同时看两类日志：
  - 生命周期日志（进程是否活着）
  - 请求级日志（每次请求状态码）

检查清单

- 是否有 `REQ ...` 和 `RES ... status=...` 成对日志。
- 是否出现大量 4xx/5xx。
- 失败时间点是否能在日志中对上。

### 问题 2：脚本“支持 fetch”，不等于“支持稳定 async/await”

现象

- 脚本能执行前半段，但状态停在“处理中”。
- `await` 之后的回填逻辑不执行。

为什么会这样

- 低代码脚本平台的运行时可能只保证同步执行阶段。
- 进入异步回调后，运行上下文可能被回收或不再继续调度。

如何快速确认

1. 在 `await` 前后分别写入标记值。
2. 若只能看到前者，说明异步链路不可靠。

正确做法

- 优先采用“同步可完成”的链路：
  - 脚本只生成下载链接
  - 用户点击链接时再由后端生成文件

检查清单

- 不要先假设 async 可用，先做 10 行最小诊断脚本。
- 平台运行时不稳定时，立刻切换同步方案，避免长期微调 async 实现。

### 问题 3：CORS 头在多层重复注入，浏览器会直接拦截

现象

- 接口返回码看似正常，但浏览器仍报 CORS 错误。
- 响应头中同一个 CORS 字段出现多次。

为什么会这样

- Flask、nginx、CDN 都在加 CORS 头时，容易重复。
- 浏览器对 CORS 头很严格，重复或冲突会当作不安全响应。

如何快速确认

1. 用 curl 抓完整响应头。
2. 统计每个 `Access-Control-*` 字段出现次数。

正确做法

- 每个 CORS 头只让一层负责。
- 在本项目中：
  - BunnyCDN 负责 Origin/Headers
  - Flask 只补 Methods
  - nginx 不再追加

检查清单

- 修改任意一层 CORS 后，都要重测 OPTIONS 和真实请求（POST/GET）。
- 结果必须是“每个 CORS 头单值且一致”。

### 问题 4：新增路径在本地通，但经过 CDN 可能 404

现象

- 直连 Flask 返回 200。
- 通过公网域名却返回 404。

为什么会这样

- CDN 的路径映射、Host 转发、回源规则可能只放通已有路径。
- 你新增了 `/gen`，但 CDN 侧未按预期转发该路径。

如何快速确认

1. 同时测三段：
   - Flask 直连
   - nginx 回环（带 Host）
   - CDN 公网地址
2. 看 CDN 头里的回源状态（如 `cdn-requestpullcode`）。

正确做法

- 优先复用已验证稳定的公网路径（本项目是 `/baoguan/generate`）。
- 非必要不新增公网路径。

检查清单

- 新路径上线前必须做“直连 + 回环 + CDN”三段验证。
- 任何一段失败都不要急着改代码，先定位是哪一层路由问题。

### 问题 5：URL 传大 payload 可能触发长度和打开限制

现象

- 某些数据能下载，某些数据点击无效或超时。

为什么会这样

- 把 JSON 放进 query 参数后，URL 会快速变长。
- 长 URL 在不同浏览器、平台、网关上容忍度不同。

如何快速确认

1. 记录每条下载链接长度。
2. 失败样本和成功样本对比长度。

正确做法

- 脚本里设置长度保护阈值，超限立即给出明确错误。
- 中长期改造为 token 短链（后端保存 payload，前端只传 token）。

检查清单

- 失败原因列必须写出“超限 + 当前长度”。
- 不要让用户只看到“失败”，而不知道为什么失败。

### [pending] 2026-05-19 — docs: 新增项目 README 与 CHANGELOG

背景

- 现场调试步骤多、问题类型多，口头结论容易丢失。
- 需要把“为什么这么做”沉淀成可复用文档。

改动位置

- `README.md`（新建）
- `CHANGELOG.md`（新建）

改动内容

- README：补齐架构、部署、运行、日志、排障、FAQ、后续优化建议。
- CHANGELOG：按时间记录每次改动、验证方式、问题总结与替代实现路径。

经验总结

- 这类联调不是“写完代码就结束”，而是“把决策过程写清楚”才算结束。
- 维护人员最容易重复出现的问题：
  - 只知道结论，不知道为什么这么选。
  - 只知道最终方案，不知道哪些方案试过且为什么放弃。
  - 只知道错误现象，不知道如何一步步定位。
- 所以文档必须至少包含三层信息：
  - 业务层：要解决什么问题。
  - 技术层：具体怎么实现。
  - 排障层：坏了以后怎么判断是哪一层坏。

同类项目可复用路径

1. 先写“运行链路图 + 责任边界”再开始联调。
2. 每次修复后立即补“验证命令 + 期望结果”。
3. 统一保留一条“最小可用 smoke test URL/命令”。

---

### [pending] 2026-05-19 — fix: 增加后端请求级访问日志

背景

- 用户反馈“下载一次后不能继续下载”。
- 仅看 systemd 生命周期日志不足以判断单次请求层面的真实失败点。

改动位置

- `scripts/backend_server.py`

改动内容

- 增加 `backend_access.log` 文件日志。
- 增加 `before_request` / `after_request` 记录：
  - method/path/remote/host/ua
  - status/took_ms
  - d 参数长度（`d_len`）
- 增加统一异常处理：记录未处理异常堆栈（HTTPException 透传）。

验证

- 连续请求同一链接多次，日志连续出现 `status=200`。
- 示例：
  - `REQ method=GET path=/generate ... d_len=15`
  - `RES method=GET path=/generate status=200 took_ms=...`

经验总结

- 这次最典型的误判是：看到 `active` 就认为“后端没问题”。
- 实际上用户关心的是“这个请求有没有成功拿到文件”，不是“进程在不在”。
- 进程状态和请求结果是两件事：
  - 进程活着：说明服务可运行。
  - 请求成功：要看每一笔请求的状态码和耗时。
- 如果没有请求级日志，排障会退化成猜测；有了请求级日志，排障会变成证据链。

同类项目可复用路径

1. 每个关键 API 默认输出请求级访问日志（路径、状态、耗时、关键参数长度）。
2. 失败先按链路分段：客户端 -> CDN -> nginx -> 应用。
3. 日志字段标准化，避免每次临时加日志。

---

### [pending] 2026-05-19 — fix: 切换为纯同步脚本，点击链接触发下载

背景

- 目标业务是“在钉钉表格中批量生成报关单，并把下载入口回填到结果表”。
- 最初方案是脚本直接请求后端（HTTP POST），后端返回下载链接，再由脚本写回 B-E 列。
- 线上表现反复异常：
  - 一开始报 `Failed to load`
  - 修完后又出现“状态长期停在处理中”
  - 再修 CORS 后，仍有“脚本看似执行，但回填不完整”
- 结论：核心矛盾不是单点接口错误，而是“低代码脚本运行时对异步链路不稳定”。

原始故障现象（按排查顺序）

1. 同步 XHR 直接失败

- 现象：脚本运行后立刻报 `Failed to load`。
- 影响：无法完成后端调用。

2. 改为 `fetch + async/await` 后仍卡在处理中

- 现象：D 列写入“处理中”后不再变化，E 列有时为空。
- 关键判断：如果 E 列也没有错误信息，通常表示 `await` 后代码没有继续执行，而不是接口返回失败。

3. CORS 重复头问题被修复后，仍存在运行时不确定性

- 现象：用 curl 验证接口已正常，脚本端仍出现回填不稳定。
- 说明：后端可用 != 脚本异步回调稳定可达。

为什么会这样（原理解释）

- 这类平台里有两套“是否可用”概念：
  - 接口可用：服务端响应正确（curl 200、响应头正确）。
  - 运行时可用：平台脚本能稳定走完异步生命周期。
- 在本项目里，接口层问题（CORS）修完后，异步生命周期问题仍在，所以业务闭环仍不稳定。
- 因此必须把方案从“异步调用后回填”改成“同步可控链路”。

改动位置

- `scripts/dingtalk_demo.js`
- `scripts/backend_server.py`

关键决策（为什么选择“纯同步 + 点击下载”）

1. 业务第一优先级是“稳定可用”，不是“调用链形式更简洁”。
2. 只要脚本内涉及异步回调，就存在平台运行时不确定性。
3. 把“调用后端”的动作从脚本执行期，延后到“用户点击链接”时，由浏览器完成，稳定性更高。

具体改动（可对照代码复盘）

1. 脚本侧：去掉所有网络调用

- 删除脚本内的后端 HTTP 调用逻辑（不再 `fetch`）。
- 保留数据筛选逻辑：按系统号码聚合主表数据。

2. 脚本侧：改为生成下载链接

- 将 `{"rows": matchedRows}` 序列化后做 URL-safe base64。
- 生成链接：
  - `https://pkcellsolution.com/baoguan/generate?d=<encoded>`
- 将“点击下载”超链接写入 B 列，状态写“链接已生成”。

3. 后端侧：`/generate` 同时支持 GET 和 POST

- `POST /generate`：保留旧行为，兼容历史调用。
- `GET /generate?d=`：解码 `d` 参数后直接生成并返回文件流。

4. 增加可观测性与防呆

- 脚本增加 URL 超长保护（超阈值直接写失败原因）。
- 脚本增加 `setHyperlink` 异常捕获，失败信息写入 E 列。
- 后端增加请求级访问日志，记录 path/status/took_ms/d_len。

实施时间线（复盘用）

1. 阶段 A：同步 XHR -> 失败（平台禁用）。
2. 阶段 B：改 async fetch -> 仍不稳定（处理中卡住）。
3. 阶段 C：修 CORS 重复头 -> 接口层恢复正常。
4. 阶段 D：确认脚本异步链路不稳定 -> 方案切换为纯同步链接。
5. 阶段 E：后端扩展 `GET /generate?d=` + 日志补齐 -> 稳定运行。

验证

1. 链路验证

- 同一下载链接重复访问多次，返回持续为 200。
- `generated/` 目录持续新增文件，说明后端可持续工作。

2. 日志验证

- `backend_access.log` 出现成对 REQ/RES，且状态码稳定。
- 未出现“第一次 200，后续系统性失败”的证据。

3. 业务验证

- 结果表可稳定写入“点击下载”与“链接已生成”。
- 点击后可正常下载，不依赖脚本异步回调。

经验总结

- 经验 1：先区分“接口可用”和“运行时可用”
- 只测 curl 的 200 不够，必须测脚本端闭环是否完成（读取 -> 处理 -> 回填）。

- 经验 2：故障排查要按链路分层，不要混在一起改
- 建议固定顺序：脚本执行 -> CDN -> nginx -> Flask -> 文件生成。
- 每修一层都保留“验证命令 + 结果”，避免回归后无法定位。

- 经验 3：当平台异步模型不稳定时，优先改产品路径而非继续微调代码
- 与其反复改 Promise 细节，不如改为同步可控的交互路径（本次就是“生成链接后点击下载”）。

- 经验 4：失败信息必须落到表格里
- 用户看不到日志，E 列就是一线诊断入口。
- 没有失败原因回填，现场会进入“缺少明确错误但无法完成业务操作”的状态。

同类项目可复用路径

1. 路径 A（本次采用，优先推荐）

- 方式：脚本只做同步逻辑，用户点击链接触发后端生成。
- 优点：稳定、实现快、对平台异步能力要求低。
- 缺点：URL 可能过长，需要长度保护或后续短链化。

2. 路径 B（平台工作流化）

- 方式：脚本只写“待处理记录”，由平台自动化/工作流执行异步调用并回填。
- 优点：可观测、可重试、职责清晰。
- 缺点：平台配置复杂，跨团队协作成本高。

3. 路径 C（服务端任务队列化）

- 方式：脚本提交任务，后端异步处理，前端轮询或回写状态。
- 优点：可扩展、适合大批量。
- 缺点：开发成本高，需要任务状态机与重试策略。

下次遇到同类问题的最小行动清单

1. 先写 10 行诊断脚本验证 async 生命周期，不通过就不走 async 方案。
2. 首日加请求级日志（至少 path/status/took_ms）。
3. 对外只保留一个稳定入口路径，减少 CDN/网关变更面。
4. 在业务表里保留失败原因列，确保一线可诊断。

---

### [pending] 2026-05-19 — fix: CORS 重复头治理

背景

- 浏览器端请求出现 CORS 异常，表面上“请求发出”，实际被拦截。
- 发现 `Access-Control-Allow-Origin/Headers` 出现重复值。

改动位置

- `scripts/backend_server.py`
- 服务器 nginx 配置：`/www/server/panel/vhost/nginx/relay_proxy.conf`

改动内容

- 明确三层职责：
  - BunnyCDN 自动注入 `Allow-Origin`、`Allow-Headers`
  - Flask 只补 `Allow-Methods`
  - nginx 不再加 CORS 头

验证

- OPTIONS、POST 响应头均为单值（不重复）。

经验总结

- CORS 问题本质上是“责任边界不清”问题，而不是单纯语法问题。
- 当多个层同时添加同一 CORS 头时，最终会形成浏览器侧冲突配置。
- 现场常见误区：只改应用代码，不检查 CDN 自动注入。
- 一旦确认 CDN 不可控，后端和网关必须收敛，避免重复声明同一头。

同类项目可复用路径

1. 先抓完整响应头，统计每个 CORS 头出现次数。
2. 先确定“谁不可控”（CDN）再分配其余层职责。
3. 保持每个 CORS 头只由一层负责。

---

### [pending] 2026-05-19 — fix: 废弃同步 XHR，过渡到异步请求（后废弃）

背景

- 初版脚本使用同步 XHR，被 DingTalk 环境直接拒绝（`Failed to load`）。

改动位置

- `scripts/dingtalk_demo.js`

改动内容

- 将同步 XHR 改为 `fetch` + async/await。
- 短期解决“同步请求被禁用”问题。

后续结果

- 在目标运行时中仍出现异步生命周期问题，最终被“纯同步脚本 + 下载链接触发”方案替代。

经验总结

- 这个阶段是“必要但不充分”的过渡修复：
  - 必要：同步 XHR 在目标平台确实不被允许，必须先移除。
  - 不充分：换成 async 后，仍受运行时生命周期约束。
- 维护人员容易把“错误提示减少”误判为“问题已解决”。
- 正确判断标准是：业务闭环是否稳定完成（读取 -> 调用 -> 回填）。

同类项目可复用路径

1. 先做最小诊断脚本验证 async 生命周期。
2. 若异步模型不稳定，立刻切换同步可控路径，避免在运行时差异上持续消耗时间。

---

## 当前推荐基线（截至 2026-05-19）

1. 钉钉脚本：`scripts/dingtalk_demo.js`（纯同步）
2. 后端接口：`GET/POST /generate`
3. 网关策略：`/baoguan/generate` 作为唯一稳定公开入口
4. 日志：`backend_access.log + journalctl`

---

## 下次做类似项目的标准落地顺序

1. 先验证平台运行时能力：同步/异步、网络、超链接能力。
2. 先定唯一公开入口路径，再做 CDN/网关映射。
3. CORS 责任一次性定死（每个头只一层负责）。
4. 首日就上请求级访问日志。
5. 预留替代路径：
   - 同步直出下载
   - token 短链
   - 队列异步回填
